import React from 'react';
import { render } from '@testing-library/react';
import { JediArchiveSimulator } from './App';

test('renders learn react link', () => {
  render(<JediArchiveSimulator />);
  // ou adicione um teste real
});